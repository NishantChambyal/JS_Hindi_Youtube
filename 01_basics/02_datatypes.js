"use strict"; // use all JS code as a newer version
// alert(3+3) // it is not work here because we use node here not browser

console.log(3+3)
console.log("nishant")

// premitive data types

let a = "nishant"  //string type
let b = 5454   //number type
let c = true  // boolean type
let d = BigInt // big integer type
let e = null  // object type (stand alone value)
let f = undefined // undefine type
let g = Symbol // define uniqueness type


// object

console.log(typeof "nishant")